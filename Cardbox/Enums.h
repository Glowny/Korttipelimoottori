#pragma once

enum PACKET_ID
{
	EMPTY,
	CONTINUE,
	START_GAME,
	REQUEST_UPLOAD,
	ACCEPT_REQUEST,
	DECLINE_REQUEST,
	UPLOAD,
	TURN_CARD,
	PICK_UP_CARD,
	RELEASE_CARD,
	DRAW_FROM_DECK,
	PUT_IN_DECK_TOP,
	PUT_IN_DECK_BOT,
	SHUFFLE,
	MESSAGE,
	MOVE_SHIT,
};

enum PHASE
{
	CONNECTIONS,
	DOWNLOADS,
	GAME,
};